# INTERPRETER

Introducing **Interpreter** -- the literal content juggler.

Type this to build the project.
```shell
mvn package
```

Then run the Java executable.
```shell
java -jar target/Interpreter-version-blahblah.jar
```

<!--
With this repository, you can have metaprogramming everywhere you go.

For every language, written literally, there is a fit to level up the game.

## \<MetaPurpose\>

The purpose of this project is to en-power the charm of being META.

```
EXPLAIN:
    where "META" stands for:  "META Elevates Textual Abstraction"

    
    goto EXPLAIN
```

Wait, how did you get here? That is supposed to trap you.

---

Let's say you want 

---

The following steps, both `Build` and `Execute` require some levels of understanding to:
 - Programming Language Runtime
 - Command line
 - Building tools

If you suffer from taking in any of these, please refer to Q&A
at the bottom of this document for more detailed explanations.

## Build

The output of building should be `.jar` file.

Before that, you'll have to install `maven`, then run the following command:

```shell
mvn package
```

The output is by default located in `target/`.

---

## Execute

You might want to have `JRE 17` before running the output `jar` file.

```shell
java -jar target/Interpreter-1.0-SNAPSHOT.jar
```

---

## Q 'n A
Q: *I do not have `Maven` ready.*  
A: https://maven.apache.org/download.cgi

Q: *I do not have `Java` ready.*  
A: https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html

Q: *I don't see my question.  What should I do?*  
A: Kindly bring up an `issue` on GitHub to us, describe your issue there,
and we will add your question here along with its solution.
-->
